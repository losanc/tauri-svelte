{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "popout",
  "description": "Capability for popout panel windows",
  "windows": ["popout-*"],
  "permissions": [
    "core:default",
    "core:window:allow-close"
  ]
}
